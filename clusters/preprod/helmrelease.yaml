apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fastapi-preprod
spec:
  interval: 5m
  releaseName: fastapi-preprod

  chart:
    spec:
      chart: ./fastapi-app
      sourceRef:
        kind: GitRepository
        name: fastapi-helm-repo
        # namespace: flaskapi-preprod

  values:
    image:
      repository: ghcr.io/pauldjmetsi/fastapi-docker-app
      tag: "2c173431aee1e0ca5d176e323d57d970312d0d48"

    env:
      APP_ENV: preprod
      APP_NAME: fastapi-preprod
      FEATURE_FLAG: "true"

    secret:
      API_KEY: "preprod-secret"

    ingress:
      enabled: true
      className: kommander-traefik
      issuer: selfsigned-issuer
      appName: fastapi
      environment: preprod
      nipDomain: "10.2.0.151.nip.io"
      tlsSecret: fastapi-preprod-tls